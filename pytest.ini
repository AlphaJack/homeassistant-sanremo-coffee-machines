[pytest]
testpaths = tests
asyncio_mode = auto
# Needed so `pytest` finds custom_components. `python -m pytest` happens to work
# without it because that form puts the working directory on sys.path; the bare
# console script CI uses does not.
pythonpath = .
addopts = -q --no-header
